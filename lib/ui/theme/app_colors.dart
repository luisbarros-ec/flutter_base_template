import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Cores de Identidade (Substitua pelos valores do novo app)
  static const Color primary = Color(0xFF1E88E5); // Ex: Azul Principal
  static const Color secondary = Color(0xFFFFA000); // Ex: Laranja Secundário

  // Cores de Feedback Técnico
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFD32F2F);
  static const Color warning = Color(0xFFFFC107);
  static const Color info = Color(0xFF2196F3);

  // Cores Neutras (Fundo, Superfícies e Textos)
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color border = Color(0xFFE0E0E0);
}
